<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!-- WARNING: Eclipse auto-generated file.
              Any modifications will be overwritten.
              To include a user specific buildfile here, simply create one in the same
              directory with the processing instruction <?eclipse.ant.import?>
			  as the first entry and export the buildfile again. -->
<project basedir="." default="build" name="Monumenta">
	<tstamp>
		<format property="TODAY" pattern="yyyyMMdd-HHmmss"  locale="en,US" />
	</tstamp>

    <property environment="env"/>
    <property name="debuglevel" value="source,lines,vars"/>
    <property name="target" value="1.8"/>
    <property name="source" value="1.8"/>
    <path id="Monumenta-Bungee.classpath">
        <pathelement location="bin"/>
        <pathelement location="../api/BungeeCord.jar"/>
		<pathelement location="../api/socket4mc-2.1.3.jar"/>
    </path>

    <target name="init">
        <mkdir dir="bin"/>
        <copy includeemptydirs="false" todir="bin">
            <fileset dir="src">
                <exclude name="**/*.java"/>
            </fileset>
        </copy>
    </target>

    <target name="clean">
        <delete dir="bin"/>
    </target>

    <target depends="clean" name="cleanall"/>
    <target depends="build-subprojects,build-project" name="build"/>

    <target name="build-subprojects"/>

	<target depends="init" name="build-project">
        <echo message="${ant.project.name}: ${ant.file}"/>
        <javac debug="true" debuglevel="${debuglevel}" destdir="bin" includeantruntime="false" source="${source}" target="${target}">
			<compilerarg line="-encoding ISO-8859-1"/>
            <src path="src"/>
            <classpath refid="Monumenta-Bungee.classpath"/>
        </javac>
    </target>

	<target name="jar">
		<exec executable="git" outputproperty="git.revision" failifexecutionfails="false" errorproperty="">
			<arg value="describe"/>
			<arg value="--tags"/>
			<arg value="--always"/>
			<arg value="--dirty"/>
		</exec>
		<condition property="repository.version" value="${git.revision}" else="unknown">
			<and>
				<isset property="git.revision"/>
				<length string="${git.revision}" trim="yes" length="0" when="greater"/>
			</and>
		</condition>
		<mkdir dir="bin"/>
		<jar destfile="../Monumenta-Bungee-Plugins_${repository.version}_${TODAY}.jar" basedir="bin/">
			<manifest>
				<attribute name="Main-Class" value="bungee.project.Main"/>
			</manifest>
			<zipfileset dir="./" includes="plugin.yml" fullpath="plugin.yml"/>
		</jar>
    </target>
</project>
